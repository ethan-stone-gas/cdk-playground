config:
  # This is a test server run by team Artillery
  # It's designed to be highly scalable
  target: https://33s4u4imhjvmrtecpqs5bmd5me0fafnr.lambda-url.us-east-1.on.aws
  phases:
    - duration: 60
      arrivalRate: 1
      rampTo: 5
      name: Warm up phase
    - duration: 60
      arrivalRate: 5
      rampTo: 10
      name: Ramp up load
    - duration: 30
      arrivalRate: 10
      rampTo: 30
      name: Spike phase
  # Load & configure a couple of useful plugins
  # https://docs.art/reference/extensions
  plugins:
    ensure: {}
    apdex: {}
    metrics-by-endpoint: {}
  apdex:
    threshold: 100
  ensure:
    thresholds:
      - http.response_time.p99: 100
      - http.response_time.p95: 75
  payload:
    path: "api-keys.csv"
    fields:
      - api_key
scenarios:
  - flow:
      - loop:
          - get:
              url: "/api/hello"
              headers:
                Authorization: "Bearer {{ api_key }}"
        count: 100
